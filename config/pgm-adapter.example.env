# personal-context 适配器配置示例（§4.3 示意的可执行等价物）
# 实际配置经环境变量注入，本文件是 env 名称与语义的唯一对照。
#
# 复制为 .env 的条目不进 Git；或由 Harness/宿主进程直接注入 env。

PGM_BASE_URL=http://127.0.0.1:8787
# 必填：由 `pgm token issue` 签发（管理身份执行），只经环境变量传递
PGM_TOKEN=
# 授权项目
PGM_PROJECT=personal-agent
# 真实模型路由目的地：mtplx | omlx | lmstudio | local | cloud:kimi
# 必须与 Harness 实际选定的供应商一致；cloud:* 会触发 local_only 深度复核
PGM_DESTINATION=mtplx
# singleflight 会话键（同一会话同一任务保持一致）
PGM_SESSION=default
# outbox 落盘路径（state 目录，不进 Git）
PGM_OUTBOX_PATH=~/c-doing/c-agent/state/dsh/pgm-outbox.jsonl

# schema_version: 1 等价语义见 adapter/lib/pgm-client.js 头注与
# adr/ADR-0001-harness-integration.md；协议版本由客户端强校验
# （pgm.context.v1 / pgm.event.v1）。
